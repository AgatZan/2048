name: CI
on: 
	push
	jobs:
		build:
			runs-on: ubuntu-latest
            steps:
				- uses: actions/checkout@1
				- name: Deps
		uses: actions/setup-node@v4
        	with:
          	node-version: '22.x'
      	- run: | 
	   		npm install
		   	npx eslint
		   	npm run test
		   	npm run deploy
